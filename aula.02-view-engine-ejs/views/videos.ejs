<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>Bem vindo a página vídeos</h1>
    <h2>Aqui você pode conferir as suas playlist</h2>

    <%= playlist %>

    <% if(!playlist){%>
     <p><%= listaPlaylist %></p>
    <%}%>

    <% if(!playlist) { %>
    <% listaPlaylist.forEach((playlist) => { %>
    <p><a href="/videos/<%= playlist %>"><%= playlist %></a></p>
    <% }) %>
    <% } %>
</body>
</html>